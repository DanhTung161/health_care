import { Badge, Card, PageIntro } from "@/components/admin/AdminUI";
import { appointments, doctors, patients } from "@/app/api/_data";

export default function Appointments() {
  return <div className="mx-auto max-w-[1400px]"><PageIntro title="Appointments" action="Schedule appointment" /><div className="mb-5 grid gap-4 sm:grid-cols-3"><Card><p className="text-sm text-slate-500">Total appointments</p><p className="mt-2 text-2xl font-bold">{appointments.length}</p></Card><Card><p className="text-sm text-slate-500">Confirmed</p><p className="mt-2 text-2xl font-bold text-emerald-600">{appointments.filter((item) => item.status === "confirmed").length}</p></Card><Card><p className="text-sm text-slate-500">Scheduled</p><p className="mt-2 text-2xl font-bold text-amber-500">{appointments.filter((item) => item.status === "scheduled").length}</p></Card></div><Card><div className="mb-5 flex items-center justify-between"><h3 className="font-bold">Appointment schedule</h3><button className="rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-600">September 4, 2026 ▾</button></div><div className="space-y-2">{appointments.map((appointment) => <div key={appointment.id} className="grid items-center gap-4 rounded-xl border border-slate-100 p-4 md:grid-cols-[100px_1.2fr_1.2fr_1.5fr_100px]"><span className="text-sm font-semibold text-blue-600">{appointment.time}</span><span className="font-semibold text-slate-800">{patients.find((item) => item.id === appointment.patientId)?.name}</span><span className="text-sm text-slate-500">{doctors.find((item) => item.id === appointment.doctorId)?.name}</span><span className="text-sm text-slate-500">{appointment.reason}</span><Badge tone={appointment.status === "confirmed" ? "green" : "amber"}>{appointment.status}</Badge></div>)}</div></Card></div>;
}
